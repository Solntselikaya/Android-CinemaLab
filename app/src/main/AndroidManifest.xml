<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/splashScreenTheme"
        tools:targetApi="31">
        <activity
            android:name=".MovieActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.collectionactions.create.CollectionCreateActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.collectionactions.edit.CollectionEditActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.collectionactions.create.createcollectionscreen.CreateCollectionScreenActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.bottomnav.BottomNavActivity"
            android:exported="false"
            android:label="@string/title_activity_bottom_nav" />
        <activity
            android:name=".presentation.auth.AuthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>